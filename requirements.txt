# Floors are what the code needs. The trailing comment is the version this
# pipeline was last verified against, on Python 3.11.9 (2026-08-31) --
# reach for those if a fresh install behaves differently.
ijson>=3.2          # verified 3.5.0 (yajl2_c backend active)
pandas>=2.0,<4      # verified 3.0.3
pyarrow>=14.0       # verified 24.0.0
requests>=2.28      # verified 2.34.2
duckdb>=0.10        # verified 1.5.4
